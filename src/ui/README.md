# Local AI UI

Frontend-only experience for:

1. Offline mode (consumes `offline_status` / `GET /offline/status`)
2. AI image detection (consumes `POST /detect-ai-image`)

User guide (French): [docs/guide-utilisateur.md](./docs/guide-utilisateur.md)

This folder does not contain the offline engine, the ML detector, or training scripts.
